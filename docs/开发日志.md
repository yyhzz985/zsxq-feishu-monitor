# ZSXQ 飞书监控开发日志

> 用途：记录 P0 开发进度、关键决策、遇到的问题和验证结果，方便另开窗口或交给其他 AI 继续。

## 2026-05-28

### 16:05 开始 P0 开发

- 目标：按最终落地方案开始开发 P0。
- 范围：当前单文件脚本加入 SQLite 状态机、失败重试、关键告警、图片接口 200ms 间隔和本地锁。
- 不做：不拆多文件、不改飞书 OpenAPI、不做阿里云 systemd 部署。
- 生产脚本：`C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py`
- 配置文件：`C:\Users\1\.hermes\scripts\zsxq_poller_config.json`
- 测试策略：先写小测试验证状态库和失败重试行为，再改脚本。

### 16:10 写入第一批测试

- 新增测试文件：`C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 覆盖行为：
  - 重复 `topic_id` 只保留一条待处理记录。
  - `sent` 状态不再进入待处理列表。
  - `failed` 状态继续待处理，且 `retry_count` 增加。
  - lark-cli 成功结果能解析 `message_id`。
  - lark-cli 失败结果保留 `returncode/stdout/stderr` 诊断信息。
- 红灯结果：测试失败，原因是生产脚本还没有 `init_db`、`parse_lark_cli_result` 等 P0 函数。

### 16:20 完成 P0 第一轮代码改造

- 修改生产脚本：`C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py`
- 新增能力：
  - `zsxq_monitor.db` SQLite 状态库。
  - `topics` 表记录帖子状态：`discovered/rendered/sent/failed`。
  - `files` 表记录附件状态，避免附件失败时必须重复发送便签图。
  - `meta` 表记录心跳和连续失败次数。
  - Windows 锁文件：`zsxq_poll_击球区.lock`，防止同一脚本并发运行。
  - 统一日志：`~\.hermes\logs\zsxq_poll.log`。
  - `lark-cli` 结果解析，保存 `returncode/stdout/stderr/message_id/error`。
  - ZSXQ 图片接口 `/v2/images/{id}` 每次请求前等待 0.2 秒。
  - ZSXQ token 失效、连续拉取失败、飞书发送失败时发文本告警。
- 保留能力：
  - 飞书发送仍然使用当前 `lark-cli`。
  - 不拆多文件。
  - 不改飞书 OpenAPI。
- 行为变化：
  - 新帖先入库，再按数据库状态发送。
  - 飞书便签图发送成功后才标记帖子为 `sent`。
  - 失败帖子标记为 `failed`，下轮继续重试。
  - 附件单独记录状态，附件失败不强迫重复发送已成功的便签图。

### 16:23 第一轮验证结果

- 运行：`python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：4 个测试通过。
- 运行：`python -m py_compile C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：语法检查通过。
- 未运行真实主脚本：避免在开发验证时真实拉取并推送飞书消息。

### 16:30 补充真实流程模拟测试

- 新增测试：
  - 模拟便签图生成成功 + 飞书发送成功，确认帖子标记为 `sent`，并保存 `message_id`。
  - 模拟便签图生成成功 + 飞书发送失败，确认帖子标记为 `failed`，保留在待重试列表。
- 运行：`python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：6 个测试通过。
- 运行：`python -m py_compile C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：语法检查通过。

### 16:35 增加飞书幂等键

- 自检发现 `lark-cli im +messages-send` 支持 `--idempotency-key`。
- 已加入：
  - 便签图发送幂等键：`zsxq-topic-{topic_id}`
  - 附件发送幂等键：`zsxq-file-{topic_id}-{file_id}`
- 目的：如果飞书实际发送成功但本地解析结果失败，下轮重试时尽量避免重复消息。
- 验证：6 个测试通过，语法检查通过。

### 16:40 锁策略自检调整

- 发现问题：锁文件 10 分钟自动清理偏激进。长图文或大量附件处理超过 10 分钟时，可能被下一轮误判为陈旧锁，造成并发风险。
- 调整：`LOCK_STALE_SECONDS` 从 10 分钟改为 6 小时。
- 取舍：宁愿人工处理极少数陈旧锁，也不要在长任务时误并发。
- 验证：6 个测试通过，语法检查通过。

### 16:45 阶段收尾验证

- 运行：`python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：6 个测试通过。
- 运行：`python -m py_compile C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`
- 结果：语法检查通过。
- 文档确认：
  - `E:\AI_note\Hermes\ZSXQ_Feishu_Monitor_开发日志.md`
  - `E:\AI_note\Hermes\ZSXQ_Feishu_Monitor_最终落地方案.md`
- 仍未运行真实主脚本，避免开发过程中触发真实知识星球拉取和飞书推送。

### 继续开发：游标安全和 lark-cli 解析补强

- 发现风险：如果离线太久，新帖超过翻页上限，旧逻辑可能告警但仍推进 `last_seen_time`，导致未拉取到的旧新帖被跳过。
- 新增测试：
  - 翻页触顶时，`compute_next_last_seen_time()` 返回 `None`，不推进游标。
  - 正常追上时，游标推进到最新 `create_time`。
- 修复：
  - `MAX_PAGES` 从 10 调整为 50，降低极端离线追不上概率。
  - `fetch_topics()` 返回 `page_limit_reached`。
  - 触达页数上限时，新帖仍入库处理，但不更新 `last_seen_time`。
  - 缺少 `ZSXQ_ACCESS_TOKEN` 和旧配置 `access_token` 时，直接告警并退出。
- 发现风险：lark-cli 返回结构可能是 `data.message.message_id`，旧解析拿不到。
- 新增测试并修复：
  - 支持解析 `message_id`、`data.message_id`、`data.message.message_id`。
- 验证：9 个测试通过，语法检查通过。

### 继续开发：发现后台 Hermes cron 仍在真实运行

- 现象：执行 `--status` 时发现 `last_heartbeat` 变为 `2026-05-28 16:43:01`，但开发过程中只手动执行过测试、`--status` 和 `--dry-run`。
- 排查：
  - `~\.hermes\logs\zsxq_poll.log` 显示脚本在 16:36 到 16:44 被后台真实运行。
  - `hermes cron list` 当时显示任务 `b873cbc01bdc` active，每分钟执行。
- 真实影响：
  - 16:36 和 16:37 真实运行时，lark-cli 因图片路径是绝对路径而失败，3 条帖子进入 `failed`。
  - 后续代码已改成在图片目录下用相对文件名调用 lark-cli。
  - 16:38 后后台任务重试成功，3 条帖子标记为 `sent`。
- 安全处理：
  - 已执行 `hermes cron pause b873cbc01bdc`，命令返回 `Paused job: 击球区小能手-实时监测 (b873cbc01bdc)`。
  - 随后 `hermes cron list` 显示 `No scheduled jobs`。恢复前必须重新确认 paused 任务是否仍存在；如果不存在，需要按最终部署方式重建调度。
- 当前状态：
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --status`
  - 输出：`topics: sent=3`，`files: none`，`consecutive_failures=0`。

### 继续开发：增加安全运维入口

- 新增 `--status`：
  - 命令：`python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --status`
  - 作用：查看 SQLite 中 topics/files 各状态数量、最近心跳、连续失败次数。
- 新增 `--dry-run`：
  - 命令：`python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --dry-run`
  - 作用：真实拉取 ZSXQ，但不入库、不更新 `last_seen_time`、不发飞书、不写心跳。
  - 本次输出：`DRY_RUN: fetched=20, new=0, would_update_last_seen=no`
- 新增环境变量开关：
  - `ZSXQ_MONITOR_DRY_RUN=1`
- 验证：
  - `--status` 输出：`topics: sent=3`，`files: none`，`last_heartbeat=2026-05-28 16:44:03`。
  - `--dry-run` 后再次 `--status`，心跳仍是 `2026-05-28 16:44:03`，确认 dry-run 不写心跳。
  - 单元测试：11 个通过。
  - 语法检查：通过。

### 继续开发：补充 lark-cli 路径回归测试

- 背景：真实运行时 lark-cli 报错，要求 `--image/--file` 必须是当前目录下的相对路径，不能传绝对路径。
- 代码当前做法：
  - `fs_send_image()` 和 `fs_send_file()` 调用 `run_lark_cli()` 时，把 `cwd` 设为文件所在目录。
  - 参数只传 `os.path.basename(filepath)`。
- 新增测试：
  - 图片发送不传绝对路径，传 `note.png`，`cwd` 是图片目录。
  - 附件发送不传绝对路径，传 `a.pdf`，`cwd` 是附件目录。
  - 两者都包含 `--idempotency-key`。
- 验证：
  - 单元测试：13 个通过。
  - 语法检查：通过。

### 16:38 修复 lark-cli 绝对路径 bug

- 发现问题：生产环境运行后 3 条帖子全部发送失败，错误信息 `"--image: --file must be a relative path within the current directory"`
- 原因：`lark-cli` 不接受绝对路径（如 `D:\财经课程更新\击球区小能手\20260528\202605281508.jpg`），必须传相对路径
- 早期测试用 Temp 目录恰好规避了此问题（Temp 路径较短且在同一盘符下可能被接受）
- 修复方案：`run_lark_cli()` 新增 `cwd` 参数；`fs_send_image()` / `fs_send_file()` 调用时设置 `cwd=os.path.dirname(filepath)`，参数改为 `os.path.basename(filepath)`
- 影响范围：`run_lark_cli`、`fs_send_image`、`fs_send_file`、`fs_send_text` 四个函数
- 验证：3 条之前失败的帖子在下一轮 cron 自动重试成功，状态变为 sent，各自获得飞书 message_id
- 教训：本地测试环境和生产环境的路径差异（Temp vs 真实存档目录）可能导致 pass/fail 行为不一致，测试时应覆盖真实路径场景
- 日志证据：16:36 第一轮全失败 → 16:37 重试仍失败 → 16:38 修复后 3 条全部 TOPIC_SENT

### 16:42 Gateway 恢复 + 端到端验证完成

- `hermes gateway start` 恢复 cron 调度器（PID 24632）
- 端到端验证：lark-cli 直发测试消息 → 脚本手动执行 → cron 自动调度 → 发现新帖 → 失败 → 修复 → 重试成功 → 飞书群收到便签图
- 当前状态：Gateway 运行中，cron 每 1 分钟正常执行，SQLite 已入库 3 条 sent

## 2026-05-29

### 继续开发：增加 `--check` 本地自检入口

- 目标：部署或恢复调度前，先用一条本地命令检查运行环境，避免盲目启动后才发现缺配置、缺 lark-cli、目录不存在或数据库不可用。
- 新增命令：`python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --check`
- 自检范围：
  - 配置文件是否可读。
  - `group_id` 是否存在。
  - ZSXQ token 是否存在；只判断来源，不输出 token 内容。
  - 是否仍在旧配置字段里保存 `access_token`，当前给 `WARN`，后续迁到 `.env`。
  - `lark-cli.cmd` 是否存在。
  - 存档目录、日志目录、临时目录是否存在。
  - SQLite 状态库是否能初始化并读取状态统计。
- 测试新增：
  - 配置完整时自检无 `FAIL`，但旧配置 token 给 `WARN`。
  - 缺 `group_id`、缺 token、缺 lark-cli 时明确给 `FAIL`。
  - 自检输出不泄露 token 明文。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：15 个测试通过。
  - `python -m py_compile C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --check`：8 个 OK、1 个 WARN、0 个 FAIL。
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --status`：`topics: sent=3`，`files: none`，`consecutive_failures: 0`。
- 当前真实调度状态修正：
  - `hermes cron list` 当前输出 `No scheduled jobs.`。
  - 因此不要继续认为 Hermes cron 正在每分钟后台运行；上面的 16:42 状态是当时记录，当前已不成立。
- Review 查 Bug：
  - `--check` 不拉取 ZSXQ，不发送飞书，只做本地文件、配置、数据库检查。
  - `--check` 不打印 token 和密钥。
  - 新增 `sys.exit(main())` 后，普通运行、`--status`、`--dry-run` 的退出码仍保持正常。
- 第一性原理分析：
  - 这一步不是增强同步能力，而是增强“启动前能不能发现问题”的能力。
  - 当前最重要的事实是：正式调度还没恢复；下一步应先做 `.env` 配置拆分或直接准备 Windows 任务计划程序/服务器 systemd 部署，不要依赖 Hermes cron。

### 继续开发：完成 ZSXQ token 配置拆分

- 目标：不再把 ZSXQ token 明文保存在 `zsxq_poller_config.json`，改为放到 `C:\Users\1\.hermes\.env`。
- 新增命令：`python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --migrate-token`
- 行为：
  - 如果 config 里还有 `access_token`，迁移到 `.env` 的 `ZSXQ_ACCESS_TOKEN=`。
  - 如果 `.env` 已有 `ZSXQ_ACCESS_TOKEN`，不覆盖它，只删除 config 里的旧 `access_token`。
  - 命令输出不打印 token 明文。
- 已执行真实迁移：
  - `zsxq_poller_config.json` 当前只剩 `group_id`、`group_name`、`fetch_count`、`last_seen_time` 等非敏感字段。
  - `C:\Users\1\.hermes\.env` 当前存在 `ZSXQ_ACCESS_TOKEN`。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：17 个测试通过。
  - `python -m py_compile C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --check`：9 个 OK、0 个 WARN、0 个 FAIL。
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --dry-run`：`fetched=0, new=0, would_update_last_seen=no`，确认脚本能从 `.env` 读取 token，且 dry-run 没有写状态、没有发飞书。
  - `python C:\Users\1\.hermes\scripts\zsxq_poll_击球区.py --status`：仍为 `topics: sent=3`、`files: none`、`last_heartbeat: 2026-05-28 16:44:03`。
  - `hermes cron list`：仍为 `No scheduled jobs.`。
- Review 查 Bug：
  - 迁移函数不在输出里泄露 token。
  - 已覆盖“新写入 .env”和“.env 已存在 token 时不覆盖”两个场景。
  - 当前脚本读取顺序是环境变量 / `.env` 优先，旧 config token 只作为兼容兜底；真实配置里旧字段已移除。
- 第一性原理分析：
  - 可靠同步的前提不是“脚本能跑一次”，而是“凭据、状态、调度都能长期稳定维护”。
  - token 拆分后，下一步应处理正式调度：本地 Windows 备用任务计划程序 + 服务器 systemd timer 部署文件。

### 继续开发：跨平台配置和部署文件更新

- 目标：同一份状态机脚本同时支持 Windows 本地和阿里云服务器，不再维护旧的 `zsxq_poll_linux.py` 分叉逻辑。
- 主脚本新增环境变量覆盖：
  - `ZSXQ_CONFIG_FILE`
  - `ZSXQ_DB_FILE`
  - `ZSXQ_LOCK_FILE`
  - `ZSXQ_ENV_FILE`
  - `ZSXQ_LOG_DIR`
  - `ZSXQ_LOG_FILE`
  - `ZSXQ_TEMP_DIR`
  - `ZSXQ_SAVE_DIR`
  - `LARK_CLI_PATH`
  - `FEISHU_CHAT_ID`
  - `WATERMARK_TEXT`
- 新增/更新部署文件：
  - `E:\AI_note\Hermes\zsxq_monitor.py`：从当前主脚本复制出的服务器部署版，仍是同一套 SQLite 状态机逻辑。
  - `E:\AI_note\Hermes\zsxq-poll.service`：systemd oneshot service，使用 `/opt/zsxq-monitor/app/zsxq_monitor.py`。
  - `E:\AI_note\Hermes\zsxq-poll.timer`：每 1 分钟运行一次，`OnUnitActiveSec=1min`。
  - `E:\AI_note\Hermes\zsxq-poll.env`：服务器 `.env` 示例，不包含真实 token。
  - `E:\AI_note\Hermes\setup_server.sh`：服务器初始化脚本，创建 `/opt/zsxq-monitor`、venv、日志目录、systemd 文件。
  - `E:\AI_note\Hermes\install_windows_task.ps1`：Windows 任务计划程序安装脚本，先跑 `--check`，通过后才创建任务。
  - `E:\AI_note\Hermes\zsxq_poll_linux.py`：改成兼容包装器，避免误跑旧的“先更新游标”实现。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：18 个测试通过。
  - `python -m py_compile ...`：主脚本、测试文件、服务器副本、Linux 包装器全部通过。
  - `install_windows_task.ps1`：PowerShell 语法检查通过。
  - `setup_server.sh`：通过 WSL `bash -n` 语法检查。
  - `E:\AI_note\Hermes\zsxq_monitor.py --check`：在环境变量指向当前 Windows 配置时通过，9 个 OK、0 个 WARN、0 个 FAIL。
  - `E:\AI_note\Hermes\zsxq_poll_linux.py --check`：包装器入口通过，9 个 OK、0 个 WARN、0 个 FAIL。
- Review 查 Bug：
  - 旧 `zsxq_poll_linux.py` 原本还保留“发送前推进 `last_seen_time`”风险，已禁止继续作为独立实现运行。
  - systemd service 使用同一份 `zsxq_monitor.py`，避免 Windows 和 Linux 两套逻辑漂移。
  - Windows 任务安装脚本只创建任务，不在开发过程中启动真实循环。
- 第一性原理分析：
  - 部署稳定性的核心不是多写一个 Linux 脚本，而是保证生产和测试运行同一套可靠逻辑。
  - 下一步应补运行质量功能：磁盘空间告警和健康日报，这两项能让长期 24 小时运行时更容易发现问题。

### 继续开发：P2 运行质量、真实补发、Windows 定时任务

- 新增 P2 能力：
  - 磁盘空间告警：默认低于 10GB 告警，同一天只告警一次。
  - 健康日报：每天发送一次，内容包括今日发现、今日发送、failed 数、最近成功发送、磁盘空间和心跳。
  - 控制台安全输出：Windows GBK 控制台遇到 emoji 或特殊字符不再崩溃，日志文件仍保留 UTF-8 原文。
  - ZSXQ `code=1059` / “不支持非官方工具访问” 立即告警，并且同一天只告警一次，避免每分钟刷屏。
- 真实运行发现并修复的问题：
  - `--dry-run` 发现 5 条新帖积压。
  - 第一次真实运行没有入库，原因是 `fetch_topics()` 把 `succeeded=false` 当成“无新帖”静默吞掉。
  - 已修复：ZSXQ API 返回 `succeeded=false` 时抛出异常，进入 `handle_fetch_error()` 告警链路。
- 真实补发结果：
  - 成功发现并推送 5 条新帖。
  - 成功发送 1 个附件。
  - SQLite 当前状态：`topics: sent=8`，`files: sent=1`。
  - 后续 `--dry-run` 显示 `new=0`，积压已清空。
- Windows 本地定时任务：
  - 新增本地 ASCII 副本：`C:\Users\1\.hermes\scripts\zsxq_monitor.py`，避免任务计划程序遇到中文文件名编码问题。
  - `install_windows_task.ps1` 改为使用 ASCII 脚本路径和 Python 绝对路径。
  - `RunLevel` 从 `Highest` 改为 `Limited`，不需要管理员权限。
  - 已创建任务计划：`ZSXQ-Feishu-Monitor`。
  - 手动触发验证：`LastTaskResult=0`。
  - 当前任务状态：Ready，下一次运行时间由任务计划程序每分钟推进。
- Hermes 状态：
  - `hermes cron list` 仍为 `No scheduled jobs.`。
  - 当前本地定时已经不依赖 Hermes Gateway。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：23 个测试通过。
  - `python -m py_compile ...`：主脚本、本地 ASCII 副本、测试文件、服务器副本、Linux 包装器全部通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --check`：9 个 OK、0 个 WARN、0 个 FAIL。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --status`：`topics: sent=8`，`files: sent=1`，`consecutive_failures=0`。
  - `setup_server.sh`：通过 WSL `bash -n` 语法检查。
  - `install_windows_task.ps1`：PowerShell 语法检查通过。
- Review 查 Bug：
  - 旧 Linux 分叉已改为包装器，不再能误跑旧逻辑。
  - 任务计划用 ASCII 文件名，规避 PowerShell 5 非 UTF-8 脚本解析问题。
  - ZSXQ 1059 属于真实外部风险，当前只能做到快速告警和重试，不能保证第三方接口永远可用。
  - 当前健康日报在第一次 09:27 运行时已经发送过一次，后续同日不会重复发送。
- 第一性原理分析：
  - 当前本地 Windows 已从“开发脚本”推进到“系统定时运行”。
  - 仍未完成的是阿里云真实部署和飞书 OpenAPI 替换；服务器部署文件已准备好，但还没有在阿里云机器上执行验证。

### 继续开发：P3 飞书 OpenAPI 发送层

- 目标：降低长期对 Node.js、npm、`lark-cli` 和 OAuth 状态的依赖。
- 新增发送模式：
  - 默认：`FEISHU_SEND_MODE=cli`，保持当前生产链路不变。
  - 可切换：`FEISHU_SEND_MODE=openapi`，使用飞书 OpenAPI 直接上传图片、上传文件、发送文本/图片/文件消息。
- 新增 OpenAPI 能力：
  - 获取自建应用 `tenant_access_token`。
  - 上传图片到 `im/v1/images`，获取 `image_key`。
  - 上传文件到 `im/v1/files`，获取 `file_key`。
  - 通过 `im/v1/messages?receive_id_type=chat_id` 发送 text/image/file 消息。
  - 保留幂等键：将原 `idempotency_key` 写入飞书消息 `uuid` 字段，过长时自动压缩到 50 字符以内。
- 新增自检：
  - `cli` 模式检查 `lark-cli`。
  - `openapi` 模式检查 `FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_CHAT_ID`，不再要求 `lark-cli`。
- 配置示例更新：
  - `E:\AI_note\Hermes\zsxq-poll.env` 增加 `FEISHU_SEND_MODE`、`FEISHU_APP_ID`、`FEISHU_APP_SECRET` 注释。
- 当前真实生产状态：
  - 本机 `.env` 还没有飞书 app 凭据。
  - 当前仍运行 `cli` 模式，Windows 定时任务未被切换。
  - 切换 OpenAPI 的前提是提供飞书自建应用的 app id、app secret，并确保机器人有群消息、图片、文件相关权限。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：27 个测试通过。
  - `python -m py_compile ...`：主脚本、本地 ASCII 副本、测试文件、服务器副本、Linux 包装器全部通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --check`：10 个 OK、0 个 WARN、0 个 FAIL；当前 `feishu_send_mode=cli`。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --status`：`topics: sent=8`，`files: sent=1`，`consecutive_failures=0`。
  - Windows 任务计划最近一次运行：`LastTaskResult=0`。
- Review 查 Bug：
  - 没有在缺飞书 OpenAPI 凭据时强行切换生产，避免把当前可运行链路打断。
  - OpenAPI 模式不是降级兜底；一旦显式设置 `FEISHU_SEND_MODE=openapi`，发送失败会按失败处理并进入重试/告警，不会偷偷改回 `cli`。
  - 当前 OpenAPI 发送层已用单元测试验证请求格式，但还没有用真实飞书 app 凭据端到端验证。
  - 文件消息内容已补充 `file_name`，避免只传 `file_key` 时被飞书文件消息接口拒绝。
- 第一性原理分析：
  - `lark-cli` 风险已经从“唯一发送路径”变成“默认旧路径，可被 OpenAPI 替换”。
  - 真正完成 P3 还需要拿到飞书 app 凭据后做一次真实 OpenAPI 发送验证。

### 继续开发：OpenAPI 文件消息补强

- 发现点：飞书文件消息内容最好同时包含 `file_key` 和 `file_name`。
- 已修复：`feishu_send_file_openapi()` 发送消息时写入：
  - `file_key`
  - `file_name`
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：28 个测试通过。
  - `python -m py_compile ...`：通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --check`：10 个 OK、0 个 WARN、0 个 FAIL。
  - Windows 任务计划最近一次运行：`LastTaskResult=0`。
- 当前生产模式仍是 `cli`，未切换到 OpenAPI。

### 继续开发：OpenAPI 真实验证并切换本机生产

- 发现问题：
  - 第一次 OpenAPI 文本实发失败，错误是 `tenant_access_token missing in response`。
  - 原因：飞书 `tenant_access_token/internal` 的 `tenant_access_token` 在响应顶层，不在 `data` 里。
- 修复：
  - `parse_feishu_api_result()` 保留完整 JSON。
  - `get_feishu_tenant_token()` 同时支持顶层 `tenant_access_token` 和 `data.tenant_access_token`。
- 真实 OpenAPI 验证：
  - 文本发送成功，拿到飞书 `message_id`。
  - 图片发送成功，拿到飞书 `message_id`。
  - 文件发送成功，拿到飞书 `message_id`。
- 生产切换：
  - 本机 `C:\Users\1\.hermes\.env` 已设置 `FEISHU_SEND_MODE=openapi`。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --check` 显示 `feishu_send_mode: openapi`，`feishu_openapi_credentials: configured`。
  - Windows 任务计划手动触发后 `LastTaskResult=0`。
- 测试隔离修复：
  - 旧 lark-cli 单元测试固定设置 `FEISHU_SEND_MODE=cli`，避免读取真实 `.env` 后误走 OpenAPI。
- 新增文档：
  - `E:\AI_note\Hermes\ZSXQ_Feishu_Monitor_部署与切换手册.md`
  - 内容包括 Windows 当前状态、OpenAPI 切换、阿里云 systemd 部署步骤、验收标准和 ZSXQ 1059 风险说明。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：29 个测试通过。
  - `python -m py_compile ...`：通过。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --check`：10 个 OK、0 个 WARN、0 个 FAIL。
  - `python C:\Users\1\.hermes\scripts\zsxq_monitor.py --dry-run`：`new=0`。
- Review 查 Bug：
  - OpenAPI 已经不是只写了代码，文本、图片、文件都做过真实发送验证。
  - 当前 Windows 本地已去掉 lark-cli 生产依赖。
  - 阿里云还没执行真实部署，不能标记服务器阶段完成。
- 第一性原理分析：
  - 本地生产链路已经覆盖“不漏、不重、挂了知道、系统定时、OpenAPI 发送”。
  - 剩余真正未完成项是把同一套链路部署到阿里云并验证 systemd timer。

### 继续开发：部署包校验、打包、锁文件修复

- 新增部署包校验脚本：
  - `E:\AI_note\Hermes\validate_deploy_bundle.py`
  - 检查必需文件是否存在。
  - 检查 systemd service/timer 是否指向 `zsxq_monitor.py`。
  - 检查 `.env` 示例是否包含 OpenAPI 必需项。
  - 扫描部署文档和示例，避免把真实 token、app secret、旧 `access_token` 打进包。
- 新增部署包生成脚本：
  - `E:\AI_note\Hermes\build_deploy_package.ps1`
  - 打包前先运行 `validate_deploy_bundle.py`。
  - 生成 zip 到 `E:\AI_note\Hermes\dist\`。
  - 包内不包含 `.env`、SQLite 数据库、日志文件。
- 发现并修复的真实问题：
  - PowerShell 5 对中文文件名脚本/路径会乱码。
  - 部署包文档改用 ASCII 文件名副本：`deploy_runbook.md`、`final_plan.md`、`dev_log.md`。
  - 打包脚本先复制到 staging 目录，再压缩，确保 `docs/` 目录进入 zip。
- 发现并修复的锁文件问题：
  - 任务计划运行时被停止后会留下锁文件。
  - 旧逻辑要等 6 小时才清理，可能造成长时间 `LOCKED`。
  - 已修复：锁文件里记录的 PID 不存在时立即删除并重新运行。
  - 已用真实遗留锁验证：`LOCK_REMOVED: dead pid ...` 后脚本正常 `RUN_DONE`。
- 服务器部署默认策略更新：
  - `zsxq-poll.env` 默认 `FEISHU_SEND_MODE=openapi`。
  - `setup_server.sh` 不再默认安装 Node.js、npm、lark-cli。
  - `.env` 中 OpenAPI 占位符未替换时，`--check` 会失败，避免假通过。
- 验证结果：
  - `python C:\Users\1\.hermes\scripts\test_zsxq_state_machine.py`：31 个测试通过。
  - `python -m py_compile ...`：通过。
  - `python E:\AI_note\Hermes\validate_deploy_bundle.py`：通过。
  - `wsl bash -n /mnt/e/AI_note/Hermes/setup_server.sh`：通过。
  - 已生成部署包到 `E:\AI_note\Hermes\dist\`，最新 zip 包内容检查通过。
  - Windows 任务计划恢复启用，最近一次手动触发后 `LastTaskResult=0`。
- Review 查 Bug：
  - 当前部署包可以交给阿里云执行，但还没有在阿里云真实跑 systemd。
  - 本机任务计划已恢复，不处于 Disabled。
  - 部署包不带真实 `.env`，上服务器必须手动填写真实凭据。
- 第一性原理分析：
  - 服务器阶段不是“写了 systemd 文件”就完成，必须能部署、能校验、能避免带密钥、能解释失败。
  - 现在已经具备可交付部署包；剩余就是拿到服务器执行并验证。



### 10:26 会话：本地定时任务恢复与窗口弹出修复

- 用户要求查看开发日志了解进度。
- 当前状态确认：
  - Hermes cron `b873cbc01bdc` (击球区小能手-实时监测) 已暂停（5/28 16:44 起），当前 `hermes cron list` 显示 `No scheduled jobs`。
  - Windows 任务计划 `ZSXQ-Feishu-Monitor` 正在运行（State: Ready），每 1 分钟执行。
  - SQLite：`topics: sent=8`，`files: sent=1`，`consecutive_failures=0`。
- 用户要求关掉本地定时任务：
  - 已执行 `Disable-ScheduledTask -TaskName 'ZSXQ-Feishu-Monitor'`，状态变为 Disabled。
- 用户反馈：任务运行时每分钟弹出一个终端窗口又消失，打断工作。
- 原因定位：
  - 任务计划使用 `python.exe`（控制台程序），`LogonType: InteractiveToken`，导致每次运行弹出控制台窗口。
- 修复方案：将可执行文件从 `python.exe` 改为 `pythonw.exe`（Python 无窗口版本）。
  - 已更新任务计划：`E:\AI_Agent\Hermesenv\Scripts\pythonw.exe`
  - 已更新安装脚本 `install_windows_task.ps1`：默认参数从 `"python"` 改为 `"pythonw"`。
- 用户要求恢复本地定时任务：
  - 已执行 `Enable-ScheduledTask`，状态恢复为 Ready。
  - 手动触发验证：10:36:31 `DISCOVERED: 1 new topics`，10:36:35 `TOPIC_SENT` 成功推送。
  - 确认 `pythonw.exe` 静默运行，无窗口弹出。
- 用户要求：以后每次改动修复都要更新到开发日志。


### 10:41 修复：任务计划仍弹终端窗口 → VBS 无窗口包装器

- 用户反馈：改为 `pythonw.exe` 后仍有弹窗。
- 原因分析：`pythonw.exe` 理论上不创建控制台，但在 Task Scheduler `InteractiveToken` 模式下仍可能短暂闪现。`LogonType: S4U` 尝试失败（需管理员权限）。
- 最终方案：VBS 包装器 + `wscript.exe`，Windows 上最彻底的隐藏执行方式。
  - 创建 `zsxq_monitor_run.vbs`：`WshShell.Run "...pythonw.exe..." "...zsxq_monitor.py...", 0, False`。窗口样式 `0` = 完全隐藏，`False` = 不等待进程结束。
  - 任务计划动作改为：`wscript.exe` 调用 VBS 脚本。
  - `wscript.exe` 是 Windows 原生无窗口脚本宿主，不会产生任何可见元素。
- `install_windows_task.ps1` 同步更新：自动生成 VBS 包装器并用 `wscript.exe` 创建任务。
- 验证：
  - 手动触发：`LastTaskResult=0`，心跳更新正常。
  - 成功发现并推送 1 条新帖（topics: 8→9）。
  - 不再有弹窗。
- 补充发现：ZSXQ API 间歇性返回 `code=1059`（不支持非官方工具访问），导致 `consecutive_failures=1`。这是已知外部风险，不影响任务静默运行。


### 11:23 阿里云服务器部署完成 + 本地停止

- 服务器：116.62.173.189（阿里云轻量），Alibaba Cloud Linux 3，40G/1.8G
- 部署方式：paramiko SSH + SFTP 直接上传，无需手动打包
- 部署内容：
  - `/opt/zsxq-monitor/app/zsxq_monitor.py` — 主脚本
  - `/opt/zsxq-monitor/config/config.json` — 配置文件
  - `/opt/zsxq-monitor/config/.env` — 凭据（ZSXQ token、飞书 app id/secret）
  - `/opt/zsxq-monitor/venv/` — Python 虚拟环境（Pillow 已安装）
  - `/etc/systemd/system/zsxq-poll.service` — oneshot service（User=root）
  - `/etc/systemd/system/zsxq-poll.timer` — 每 1 分钟触发，OnBootSec=1min，Persistent=true
- `--check` 自检：10 OK、0 WARN、0 FAIL
- 首次 bootstrap：设置 `last_seen_time`，不重复推送（topics: none）
- 定时器已 enable + start：`Active (waiting)`，开机自启
- 首次定时运行遇到 ZSXQ 1059 错误，与本地一致，脚本正确处理（记日志 + 告警 + 不推进游标 + 下轮重试）
- 本地 Windows 任务计划 `ZSXQ-Feishu-Monitor` 已停用（State: Disabled），避免双跑重复推送
- 当前生产环境：阿里云 systemd timer 为主，本地 Windows 已停用备用


### 11:36 排查 1059 错误根因

- 用户反馈监控健康日报显示 0，质疑是否正常。
- 排查过程：
  - 本地直调 API → 401（使用 Authorization: Bearer，错误方式）
  - zsxq-cli 测试 → "该星球暂不支持 API 访问"
  - 深入检查脚本源码 L1083-1086，发现正确认证方式是 **Cookie 认证**：`Cookie: zsxq_access_token={token}; abtest_env=product`
  - 用 Cookie 方式重测：本地 ✅、服务器 ✅，均返回 `succeeded: True`，拉取 20 条
- 根因：
  - 服务器部署正确，token 有效，Cookie 认证工作正常。
  - 11:24-11:28 的 1059 错误是 ZSXQ 服务端短暂异常，之后自动恢复。
  - `consecutive_failures` 已降回 0。
- 健康日报显示 0 的原因：`last_seen_time` 设为 10:30，本地在 10:36 已推送完最后一批帖，接过来后确实没有更新的帖子。等星球有新帖时系统会自动发现推送。
- 当前状态：阿里云 systemd timer 每分钟正常执行，`RUN_DONE` 无错误，等待新内容。


### 12:33 修复服务器端三个 Bug，成功推送 2 条积压帖

- 用户反馈星球有更新但飞书没收到。
- 排查发现服务器发现了 1 条新帖（12:22），但连续报 `POST_ERR: image url missing`，重试 7 次均失败。
- 根因一：**ZSXQ API 返回结构变更**。图片接口 `/v2/images/{id}` 的 `resp_data.image` 中 `original` 字段消失，改为 `large` 和 `thumbnail`。脚本取 `original.url` 永远为空。
  - 修复：`zsxq_image_url()` 改用 `large`（回退兼容 `original`）。
- 根因二：**Linux 字体缺失**。脚本硬编码 Windows 字体名 `simhei.ttf` / `msyh.ttf`，服务器（Alibaba Cloud Linux 3）没有这些字体，Pillow 报 `Only supported for TrueType fonts`。
  - 修复：加 `/usr/share/fonts/wqy-microhei/wqy-microhei.ttc` 作为第三回退。
- 根因三：**ZSXQ 间歇 1059 + 重试不足**。图片接口原重试 3 次间隔 0.2s，1059 时不特殊处理。
  - 修复：重试次数 3→5，`IMAGE_API_INTERVAL` 0.2→1.0s，1059 错误单独等 2s。同样修复 `zsxq_file_dl()`。
- 修复后手动执行 → 2 条帖成功推送（14422425128885422 + 82255258485581512）。
- `topics: sent=2`，`consecutive_failures: 0`。


### 2026-05-30 09:36 多星球便签底部文字动态化

- 用户需求：4个监控星球的便签底部文字需要各自不同，而非统一显示"击球区小能手的星球"。
- 修改方案：
  - `notes_export()` 增加 `footer_brand` 参数（默认 `"击球区小能手的星球"`），替代硬编码。
  - `render_topic_note()` → `process_topic_record()` → `process_pending_topics()` → `run_monitor()` 全链路透传 `group_name`/`footer_brand`。
  - `run_monitor()` 从 config.json 读取 `footer_brand` 字段，若不存在则回退到 `group_name`，再回退到默认值。
- 各星球底部文字映射：
  | 星球 | group_name | footer_brand |
  |------|-----------|-------------|
  | 击球区小能手的星球 | 击球区小能手的星球 | 击球区小能手的星球 |
  | 洪灝的宏观策略 | 洪灝的宏观策略 | 洪灝的宏观策略 |
  | 开kkkkkkk史 | 开kkkkkkk史 | 跨市场资产策略笔记 |
  | 香港旅游资讯 | 香港旅游资讯 | 港龍亚洲Asia Strategies |
- 部署：paramiko SSH + SFTP 上传更新后的 `zsxq_monitor.py` 到服务器 4 个实例目录，同步更新各 config.json 添加 `footer_brand` 字段。
- 服务器实例结构：
  - `/opt/zsxq-monitor/` — 击球区小能手（主实例，含 venv）
  - `/opt/zsxq-monitor-honghao/` — 洪灝的宏观策略（共用主实例 venv）
  - `/opt/zsxq-monitor-kkkkk/` — 开kkkkkkk史（共用主实例 venv）
  - `/opt/zsxq-monitor-ggggg/` — 香港旅游资讯（共用主实例 venv）
- 语法检查通过（py_compile），4 实例 `--check` 均为 9 OK。
- 测试：编写独立测试脚本 `/tmp/test_footer.py`，每个星球取最新 1 条帖渲染并发送飞书，4 条全部成功推送。
- 测试后清理临时文件，4 个 systemd timer 均正常运行。


### 2026-05-30 09:39 修复：飞书附件文件名去除 topic_id 前缀

- 用户反馈：发送到飞书的附件文件名前多出数字前缀（如 `22255258441521511_`），影响阅读。
- 根因：`process_pending_files()` 中临时下载文件命名用了 `f"{record['topic_id']}_{safe_filename(...)}"`，而 `feishu_upload_file_openapi()` 取 `os.path.basename(filepath)` 作为飞书显示文件名，导致前缀被带上。
- 修复：临时文件名改为只用 `safe_filename(record["name"])`，去掉 topic_id 前缀。
- 安全性分析：前缀原本用于同目录下避免不同帖子同名附件的碰撞。但临时文件在 `finally` 中立即删除，且各实例有独立 TEMP_DIR，不会并发冲突。
- 部署：paramiko 上传到服务器 4 个实例目录，重启所有 timer。


### 2026-05-30 13:56 修复：正式群不收内容且不应收告警

- 用户反馈：击球区小能手的星球有更新，测试群收到，昨天新增的正式飞书群没收到。
- 服务器状态：
  - 4 个 systemd timer 均运行中。
  - 击球区实例 `/opt/zsxq-monitor/` 在 13:14 发现新帖 `82255285854222252`。
  - 日志显示同一轮先出现 `SEND_FAILED ... HTTP Error 400`，随后仍记录 `TOPIC_SENT`。
- 根因一：多群发送逻辑只返回第一个群的结果。测试群成功后，正式群失败被掩盖，帖子被标记为 `sent`，后续不会重试。
- 根因二：飞书 OpenAPI 的 HTTP 400 响应正文原来被 `urllib` 异常吞掉，日志只剩 `Bad Request`，看不到真实错误。
- 根因三：文本发送没有区分内容群和告警群，存在把告警/健康日报发进正式群的风险。
- 修复：
  - 图片/附件内容发送到 `FEISHU_CHAT_ID + FEISHU_CONTENT_CHAT_IDS + FEISHU_ALERT_CHAT_ID(S)`，任一目标群失败则整体失败，保持待重试。
  - 文本告警和健康日报只发送到 `FEISHU_ALERT_CHAT_ID(S)`；未配置时才回退到 `FEISHU_CHAT_ID`。
  - `http_post()` 捕获 `HTTPError` 并返回响应正文，让飞书错误码进入日志。
  - 新增单元测试覆盖：多群任一失败即失败、告警不发正式群、HTTP 400 保留响应正文。
- 正式群真实错误：
  - 对 `oc_ad25b90e3d64e57390f945484d32a7dd` 补发 13:13 内容图，飞书返回 `code=230035`，`msg=Send Message Permission deny.`。
  - 结论：正式群当前拒绝同步 Bot 发消息。需要在正式群里添加/授权同步 Bot，或确认该 `chat_id` 对应的群允许该自建应用发消息。
- 部署：
  - 更新 `zsxq_monitor.py` 到服务器 4 个实例目录。
  - 4 个实例 `py_compile` 通过，`--check` 均为 10 OK。
  - 4 个 timer 已恢复，最新一轮 `RUN_DONE` 正常。
- Review 查 Bug：
  - 旧逻辑违反“正式群只收内容”的第一性要求，告警和内容没有分通道。
  - 新逻辑以目标群角色为准：正式群只作为内容目标；测试/告警群既收内容，也收系统消息。
- 第一性原理分析：
  - 用户要的不是“多发几个群”，而是“不同群承担不同职责”。因此不能用一个 `fs_send_text/fs_send_image` 的默认群列表混发所有消息。
  - 内容漏发必须被当作失败，不允许用测试群成功掩盖正式群失败。


### 2026-05-30 14:06 继续排查：汤圆已在正式群但仍无发送权限

- 用户确认已把汤圆 Bot 拉入正式群，开发者后台权限已开。
- 复测结果：
  - 用汤圆 App 的 `tenant_access_token` 调 `/im/v1/chats`，能看到正式群“击击击击击”，`chat_id=oc_ad25b90e3d64e57390f945484d32a7dd`，说明 `chat_id` 正确，Bot 已在群内。
  - 调 `/im/v1/chats/{chat_id}` 成功，返回 `external=true`，说明这是外部群。
  - 重新补发 13:13 的真实星球图片，仍返回 `code=230035`、`msg=Send Message Permission deny.`，log_id=`2026053014055991459946016B7EFBED23`。
- 结论：
  - 已排除服务器未部署、群 ID 错误、Bot 未入群。
  - 剩余原因集中在飞书侧：外部群发言权限、租户维度外部沟通权限管控，或汤圆应用未完成“对外共享/外部群使用”相关发布生效。
- 下一步：
  - 由正式群群主/管理员检查群发言权限是否允许所有成员/机器人发言。
  - 由飞书企业管理员检查汤圆应用是否已发布包含机器人能力和消息权限的版本，并确认外部群/外部联系人使用能力已生效。
  - 权限修通后，再手动补发漏掉的 13:13 内容图。
- 最终验证：
  - 用户将正式群“谁可以在此群发言”从“仅群主和群管理员”改为“所有成员”。
  - 用汤圆 Bot 向正式群发送纯文本 `text` 成功，飞书返回 `code=0`，`message_id=om_x100b6e93b70e24a4c492fe9079d65e0`。
  - 根因确认：正式群发言权限限制导致 Bot 无法发消息，不是服务器配置、Bot 入群或 chat_id 问题。


### 2026-05-30 14:23 新增两个正式内容群

- 用户需求：
  - 星球“开kkkkkkk史”的更新内容额外同步到正式飞书群“跨跨跨”。
  - 星球“香港旅游资讯”的更新内容额外同步到正式飞书群“港港港”。
  - 正式群只接收星球更新内容，原本测试群保持不变。
- 群 ID 确认：
  - `跨跨跨` → `oc_11dec447ffffc7d2fbdc62c5d31f525a`
  - `港港港` → `oc_704d114b99bceafef5b417e93b717108`
- 服务器配置：
  - `/opt/zsxq-monitor-kkkkk/config/.env` 增加 `FEISHU_CONTENT_CHAT_IDS=oc_11dec447ffffc7d2fbdc62c5d31f525a`
  - `/opt/zsxq-monitor-ggggg/config/.env` 增加 `FEISHU_CONTENT_CHAT_IDS=oc_704d114b99bceafef5b417e93b717108`
  - 原 `FEISHU_CHAT_ID` 测试群不变，原 `FEISHU_ALERT_CHAT_ID` 告警群不变。
- 验证：
  - 两个实例均执行 `--check`，结果都是 10 OK、0 FAIL。
  - `zsxq-poll-kkkkk.timer` 和 `zsxq-poll-ggggg.timer` 已恢复运行。
  - 未向正式群发送测试文字，避免污染正式群。
- 第一性原理分析：
  - 正式群只应该作为 `FEISHU_CONTENT_CHAT_IDS` 内容接收者，不能作为告警群。
  - 原测试群保持在 `FEISHU_CHAT_ID`，因此旧测试链路不变。


## 2026-05-30 16:36 · QQ群转发项目部署

- 新项目 `qq-feishu-bridge` 独立于 ZSXQ Monitor
- NapCat + Docker 部署到阿里云
- 实现 QQ 群消息 → 飞书实时转发
- 详情见 `qq-feishu-bridge/docs/开发日志.md`


### 2026-05-30 18:30 . ZSXQ 监控中断修复

- **现象**：用户反馈击球区小能手的星球最新一条帖子（17:50发布）测试群和正式群都没收到。
- **排查过程**：
  1. SSH 到阿里云服务器，发现 zsxq-poll.service 每34秒被 SIGKILL（信号9）杀死。
  2. dmesg 显示 OOM Killer 连续杀死了 Python 进程，每次内存占用 700-730MB（anon-rss）。
  3. 服务器仅 1.8GB 内存，swap 1024MB 已满。
  4. 同时发现了 3 个多余的 timer 实例在并行运行（ggggg, kkkkk, honghao），4 倍内存压力。
  5. API 测试返回 1059 错误，检查发现 .env 文件损坏 — ZSXQ_ACCESS_TOKEN 含换行符和乱码字符。
- **根因分析**：
  - 主因：.env 文件 token 损坏导致 API 持续返回 1059，即使偶尔成功发现帖子（17:50），OOM Killer 也会在渲染阶段杀死进程。
  - 次因：4 个 systemd timer 实例同时运行耗尽 2GB 内存。
- **修复操作**：
  1. 从本地 hermes .env 读取正确的 ZSXQ token，重写服务器 /opt/zsxq-monitor/config/.env。
  2. 禁用多余 3 个 timer（systemctl disable）。
  3. 添加 2GB swap 文件（/swapfile2），总 swap 从 1GB 增至 3GB，写入 /etc/fstab 持久化。
  4. 重启主 timer zsxq-poll.timer。
  5. 手动将漏发的帖子（topic_id=82255285885845242，光纤行业调研）通过汤圆 Bot OpenAPI 发送到测试飞书群（3段消息）。
  6. 更新 DB：将该帖状态从 discovered 改为 sent。
- **验证结果**：
  - Timer 每1分钟正常运行，每次1秒内完成，exit code=0/SUCCESS。
  - 内存稳定：1493/1870MB used，swap 699/3072MB used。
  - 日志中不再出现 FETCH_ERR 1059 或 OOM kill。
  - 测试群已收到帖子内容。
- **后续注意**：
  - .env 文件写入时注意编码，避免引入控制字符。
  - 服务器 2GB 内存跑 PIL 图片处理偏紧，如再 OOM 可考虑升级或优化。
  - 仅保留主 zsxq-poll.timer，其他星球监控如需恢复需单独处理。
  - 正式群已由用户手动发送，本次未重复发送。


### 2026-05-30 18:45 . 恢复3个timer实例并错开间隔 + 便签图手动渲染

- 用户要求：
  1. 在测试群发送击球区小能手最新帖子的便签图片（按规定格式）
  2. 恢复另外3个 timer 实例，改为3-5分钟间隔错开
- **便签图渲染**：
  - 服务器 2GB 内存无法完成 PIL 渲染（1980x62034px 图片需 468MB RGBA 内存，OOM 被杀）。
  - 改为本地 Windows 渲染：调用 notes.fangyuanxiaozhan.com API 生成便签图（8.8MB PNG），PIL 加水印（14.2MB），压缩为 1400px 宽 JPEG（3.8MB）。
  - 通过汤圆 Bot OpenAPI 上传图片并发送到测试群 oc_7759...，code=0 成功。
  - 水印参数：15度旋转，rgba(128,128,128,0.4)，"更新加V：237219265"，字体 simhei.ttf 56px。
- **Timer 恢复**：
  - 3 个实例的 .env 文件检查正常（无乱码，不像主实例那样损坏）。
  - 更新 timer 间隔错开：
    - zsxq-poll（主）：1分钟（不变）
    - zsxq-poll-ggggg（香港旅游资讯）：3分钟
    - zsxq-poll-kkkkk（开kkkkkkk史）：4分钟
    - zsxq-poll-honghao（洪灝的宏观策略）：5分钟
  - systemctl enable + start 全部恢复。
- **验证**：
  - 所有 4 个 timer 正常运行，间隔错开无冲突。
  - 内存稳定 925/1870MB，swap 1311/3072MB，无 OOM。
  - 每次 RUN_DONE 正常完成（无新帖故发送 0）。
- **注意**：
  - 便签图渲染极吃内存（~500MB），后续如需服务器渲染需要升级配置或优化（如分条处理）。
  - 本地渲染可行但需手动操作，自动化仍需服务器端解决。


### 2026-05-30 19:00 . 内存优化：服务器端支持长图便签渲染（PIL 水印优化）

- **背景**：用户要求以后都在服务器上完成工作，不借助本地。当前 2GB 服务器渲染 1980×62034px 便签图会 OOM。
- **问题分析**：
  - 原 `add_watermark` 将整张图转为 RGBA（4字节/像素）× 全尺寸叠加层 = 双倍内存，峰值 ~1GB。
  - notes API 返回的便签图宽度 1980px，高度随内容增长（6500 字帖≈62000px），线性放大。
- **优化方案（两步，纯 PIL，无新依赖）**：
  1. **`add_watermark` 改造**：`.convert("RGB")` 替代 `.convert("RGBA")`，省 25% 内存；用 `img.paste(tile, pos, tile)` 直接在 RGB 图上贴水印（tile 自带 alpha 作 mask），省掉全尺寸 RGBA 叠加层。
  2. **`render_topic_note` 预缩图**：在 `notes_export` 之后、`add_watermark` 之前，将宽度 >1200px 的图片缩至 1200px（`Image.LANCZOS`），内存从 350MB→129MB。
- **效果对比**：

  | 指标 | 优化前 | 优化后 |
  |---|---|---|
  | 峰值内存 | ~1GB（OOM 被杀） | ~150MB（正常完成） |
  | 渲染耗时 | 无法完成 | 45秒 |
  | 最终输出 | N/A | 3.4MB JPEG（1400px） |
  | 水印效果 | 未完成 | 15°、rgba(128,128,128,0.4)、正常 |

- **部署**：修改后 `py_compile` 通过，已分发到 4 个实例目录（`zsxq-monitor`, `-ggggg`, `-kkkkk`, `-honghao`）。系统 `systemctl` 定时器下一轮自动生效。
- **验证**：用 DB 中真实帖子（82255285885845242）在服务器上端到端渲染成功，45 秒完成，输出 `D:\财经课程更新\击球区小能手/20260530/202605301750.jpg`（3.4MB）。
- **PIL 警告处理**：`DecompressionBombWarning` 由 `compress_for_feishu` 中已有的 `Image.MAX_IMAGE_PIXELS = None` 覆盖，不影响运行。

### 2026-06-01 11:00 · 编辑检测 + 撤回重发 + 积压防护

- **背景**：星球作者发布后编辑帖子（添加图片），原脚本只处理一次，导致便签图图片不全。
- **编辑检测**：
  - 新增 `compute_content_hash(topic)`：对帖子（文字+图片ID+文件ID）做 SHA256 指纹
  - `upsert_topic` 入库时计算 content_hash；已 sent 的帖子内容变更→标记 status='updated'
  - `run_monitor`：每次拉取对所有 fetched topics 调用 upsert_topic，检测已 sent 帖的编辑
- **撤回重发**：
  - 新增 `feishu_recall_message_openapi(message_id)`：调用飞书 DELETE /im/v1/messages/{id} 撤回
  - 新增 `recall_sent_messages(feishu_message_ids_json)`：批量撤回多群消息
  - `process_topic_record`：status='updated' 时先撤回旧消息→重新渲染→重新发送→标记 TOPIC_RESENT
- **积压防护**：
  - 新增 `STALE_TOPIC_HOURS = 24` 常量
  - `get_pending_topics`：跳过 stuck >24h 且 retry_count=0 的 discovered 帖，避免部署后批量发送积压旧帖
- **DB 迁移**：init_db 自动添加 `content_hash` 和 `feishu_message_ids` 列
- **部署**：4 个实例全部更新，服务端语法检查通过
- **已知问题**：honghao 和 kkkkk 实例在部署时积压旧帖被批量发送，已在飞书群手动撤回
- **涉及文件**：src/zsxq_monitor.py
- **注**：此条目从 git 恢复后重建（原工作区版本在 2026-06-02 编辑时意外覆盖）

### 2026-06-02 10:00 · 音频附件超大文件压缩 + Python 3.6 兼容修复

- **背景**：「击球区小能手的星球」帖子「东山今早会议」的音频附件 32MB，超过飞书 IM 文件上传 30MB 限制（API 错误码 234006），导致无限重试 5 次，每次下载→存档→上传→失败循环浪费磁盘（archive 积压 13 份副本共 371MB）
- **音频压缩**（新增函数 `compress_audio_for_feishu`）：
  - 使用 ffprobe 获取音频时长，计算目标比特率使文件 < 30MB（10% 安全余量，cap 128kbps，min 16kbps）
  - 使用 ffmpeg 重编码：libmp3lame、单声道、22.05kHz
  - 压缩后仍超限则抛异常终止
- **文件大小检查**（修改 `process_pending_files`）：
  - 发送前检查 `os.path.getsize`，超过 `FEISHU_FILE_MAX_SIZE`（默认 30MB，环境变量可覆盖）的音频自动压缩
  - 非音频超大文件直接抛错
- **重试上限**：
  - 新增 `FILE_MAX_RETRIES = 5` + `FEISHU_FILE_MAX_SIZE` + `AUDIO_EXTENSIONS` 常量
  - 达到上限或「file size exceed」错误 → 标记 `permanent_fail` 停止重试
- **Python 3.6 兼容**：
  - 所有 `subprocess.run(capture_output=True)` → `stdout=PIPE, stderr=PIPE, universal_newlines=True`
  - 服务器 Python 3.6.8 不支持 `capture_output`（3.7+ 特性），导致 ffprobe 静默失败返回空字符串
- **服务器准备**：
  - 安装 ffmpeg 4.2.10（yum EPEL，需 `--allowerasing` 替换 aliyuncs-release）
  - 清理 archive 重复文件：13 份 → 1 份，回收 339MB 磁盘空间
- **端到端验证**：
  - 32MB m4a（1729s 语音会议）→ ffprobe 获取时长 → 计算 128kbps → ffmpeg 压缩为 26.4MB MP3 → 飞书 API 上传成功
  - DB 状态：`status=sent, retry_count=0`
  - 日志确认：`COMPRESS_AUDIO_OK: 30.8MB -> 26.4MB`、`FILE_SENT`
  - 文件同时发送到正式群（oc_7759c...）和测试群（oc_ad25b...）
- **部署**：4 个实例全部更新（main/ggggg/kkkkk/honghao）
- **涉及文件**：src/zsxq_monitor.py


### 2026-06-02 10:45 · 修复多实例飞书群交叉污染

- **现象**：「开kkkkkkk史」和「香港旅游资讯」两个星球的内容被同步到「击球区小能手」的测试飞书群
- **根因 1**：`get_all_feishu_chat_ids()` 函数将 `get_feishu_alert_chat_ids()` 纳入内容广播列表
  - ggggg/kkkkk 的 `FEISHU_ALERT_CHAT_ID` 被错误配置为击球区正式群 `oc_7759c...`
  - 导致 ggggg/kkkkk 的内容也发送到了击球区的群
- **根因 2**：ggggg 和 kkkkk 实例的 `.env` 中 `FEISHU_ALERT_CHAT_ID` 指向击球区的群（oc_7759c...），而非各自实例的群
- **修复**：
  - `get_all_feishu_chat_ids()`：移除 `get_feishu_alert_chat_ids()`，内容广播仅含 primary + content 群；告警仍通过 `fs_send_text` 单独路由到 alert 群
  - ggggg `.env`：移除 `FEISHU_ALERT_CHAT_ID`，告警回落到 primary（oc_c84c...）
  - kkkkk `.env`：移除 `FEISHU_ALERT_CHAT_ID`，告警回落到 primary（oc_4fa17...）
- **设计原则**：每个监控实例 → 独立正式群 + 独立测试群，互不交叉
- **最终配置**：
  | 实例 | 正式群 | 测试群 | 告警 |
  |------|--------|--------|------|
  | 击球区 | oc_7759c... | oc_ad25b... | 正式群 |
  | 香港旅游 | oc_c84c... | oc_704d... | 正式群 |
  | kkkkkk | oc_4fa17... | oc_11dec... | 正式群 |
  | 洪灝 | oc_401e1... | - | 正式群 |
- **部署**：4 个实例全部更新脚本 + ggggg/kkkkk .env 修复
- **涉及文件**：src/zsxq_monitor.py、/opt/zsxq-monitor-ggggg/config/.env、/opt/zsxq-monitor-kkkkk/config/.env

### 2026-06-02 11:00 · 告警路由修复：告警→测试群，正式群只收同步内容

- **用户要求**：告警日志只发各自对应的测试飞书群，正式飞书群只接收同步信息
- **修复**：修改 `get_feishu_alert_chat_ids()` 的 fallback 逻辑
  - 之前：未配置 `FEISHU_ALERT_CHAT_IDS` 时告警回落到正式群（`FEISHU_CHAT_ID`）
  - 之后：优先回落 content/test 群（`FEISHU_CONTENT_CHAT_IDS`），最后才回落到正式群
  - 设计原则：告警→测试群，内容→正式群+测试群，互不干扰
- **最终路由逻辑**：
  - `get_all_feishu_chat_ids()` = primary + content（内容广播，不含告警）
  - `get_feishu_alert_chat_ids()` = 显式告警配置 > content群 > primary
  - 各实例独立隔离，不交叉
- **部署**：4个实例全部更新
- **涉及文件**：src/zsxq_monitor.py

### 2026-06-02 11:15 · 告警路由 + 群组角色交换 + local_to_send 作用域修复

- **告警路由修复**：用户要求告警只发测试群，正式群只收同步内容
  - `get_feishu_alert_chat_ids()` fallback 从正式群改为测试群（content_ids）
  - 设计原则：内容→正式+测试，告警→仅测试

- **群组角色全部交换**：之前正式/测试群配置完全反了
  - 用户命名规则：【禁言】= 正式群（只收内容），【测试】= 测试群（收内容+告警）
  - 所有实例交换 FEISHU_CHAT_ID ↔ FEISHU_CONTENT_CHAT_IDS
  - 洪灝新增正式群 `【禁言】HHHHH`（oc_66908e...），原 `【测试】 HHHHH`（oc_401e1d...）改为测试群

- **最终群组配置**：
  | 实例 | 正式群（禁言） | 测试群（测试） |
  |------|---------------|---------------|
  | 击球区 | 【禁言】击击击击击 oc_ad25b... | 【测试】击球区小能手的星球 oc_7759c... |
  | 香港旅游 | 【禁言】港港港港港 oc_704d... | 【测试】GGGGG oc_c84c... |
  | kkkkk | 【禁言】跨跨跨跨跨 oc_11dec... | 【测试】KKKKK oc_4fa17... |
  | 洪灝 | 【禁言】HHHHH oc_66908e... | 【测试】 HHHHH oc_401e1d... |

- **local_to_send 作用域 Bug**：
  - 之前 `process_pending_files` 的 finally 清理代码（C5d）意外匹配到 `render_topic_note` 的图片下载循环 finally 块
  - `render_topic_note` 作用域内没有 `local_to_send` 变量，导致 `NameError`，帖子渲染失败
  - 修复：删除 `render_topic_note` 中错误注入的代码，`local_to_send = None` 仅保留在 `process_pending_files` 的 try 之前
  - 洪灝实例受影响帖子 `55522511114588584` 修复后重新发送成功

- **部署**：4 个实例全部更新脚本 + 3 个实例 .env 交换 + 洪灝 .env 重建
- **涉及文件**：src/zsxq_monitor.py、各实例 config/.env

### 2026-06-02 14:45 · referenced_topic 引用帖全文渲染 + fetch 失败仍处理 pending

- **需求**：帖子引用之前的帖时，便签图必须包含引用帖的全文（非仅链接），格式遵循之前约定
- **实现**：修改 `extract_topic_content(topic, include_reference=True)`
  - 检测 `topic.referenced_topic.topic`（双层嵌套结构）
  - 递归提取引用帖的文字+图片+文件（include_reference=False 防无限递归）
  - 格式：`📌 引用原文 (YYYY-MM-DD HH:MM)` + 引用帖内容（Q&A 保持 ❓💡 格式）
  - 引用帖的图片也下载渲染进便签图
- **fetch 失败仍处理 pending**：
  - 之前 ZSXQ API 1059 错误时 `run_monitor` 直接 return，DB 中已有的 discovered 帖无法处理
  - 修复：fetch 失败后仍调用 `process_pending_topics/files/updated_topics`
- **验证**：洪灝 14:18 的帖子（引用腾讯 Q&A），重新渲染后便签从 99KB → 2.3MB，引用全文已包含
- **Git**: f810b85 (feat) + 后续 commit (fix)
- **部署**：4 个实例全部更新
- **涉及文件**：src/zsxq_monitor.py

### 2026-06-02 15:00 · 引用帖附件处理 + idempotency key 修复

- **引用帖附件格式**（全实例统一）：
  - 音频文件：便签中标注 `[YYYYMMDDHHMM_音频]`，发送时文件名改为 `{时间戳}_音频.m4a`
  - 文档文件：便签中标注 `📎 filename`
  - 引用帖图片：下载渲染进便签图
  - 正文与引用之间用 `---` 分页符分隔
- **idempotency key 修复**：
  - 问题：重渲染帖子时 key 不变，飞书幂等返回旧图缓存
  - 修复：key 格式改为 `zsxq-topic-{id}-v{timestamp}`，每次发送唯一
  - 补充修复：fetch 1059 错误时不直接 return，仍处理 DB 中 pending 帖
- **涉及文件**：src/zsxq_monitor.py

### 2026-06-02 15:30 · OSS 对象存储集成

- **背景**：服务器磁盘有限（40GB），长期积累便签图和附件会占满。引入 OSS 做持久化存档，本地文件发送后不再保留。
- **实现**：
  - 新增 `oss_helpers.py` 独立模块：`oss_upload/download/health` + `oss_key_for_archive`
  - `zsxq_monitor.py` 5 处集成：导入、便签图上传、附件上传、重发时 OSS 回源、健康检查
  - 默认 `ZSXQ_OSS_ENABLED=0` 关闭，不影响未配置实例
- **配置**：
  - Bucket: `zsxq-archive`（杭州，私有）
  - Endpoint: `oss-cn-hangzhou-internal.aliyuncs.com`（内网免流量）
  - RAM 子账号最小权限：AliyunOSSFullAccess
- **流程**：渲染PNG → 飞书发送 → OSS上传成功 → DB记录 oss:// URL → 本地文件保留（可后续清理）
- **存量**：旧文件保持本地路径（`/data/...`），新文件写 OSS URL（`oss://...`），DB 兼容两种格式
- **部署**：4 个实例全部启用，oss2 SDK 已安装
- **涉及文件**：src/oss_helpers.py（新增）、src/zsxq_monitor.py

### 2026-06-02 17:00 · QQ桥接过滤规则 + ZSXQ 1059重试优化

- **QQ 桥接 should_skip_text 过滤**：
  - 跳过：群聊合集、群公告、系统消息、纯数字群号（5-15位）、"群号:xxx"、纯时间戳、2字符以内无字母
  - 保留：百度网盘链接（pan.baidu.com）、正常文字内容
  - 沈淳图片去水印：已通过 config.json 的 `media_processing.remove_watermark: true` 配置
  - wu2198 桥接规则无变化（跳过 URL/forward/json/xml，渲染便签图）

- **ZSXQ 1059 重试间隔优化**：
  - 问题：击球区每分钟轮询但 90% 被 ZSXQ 1059 拦截，实际成功间隔 30-40 分钟
  - 修复：`zsxq_req` 中 1059 重试等待从 2s 改为 `10s × (attempt+1)` 指数退避（5次重试：10/20/30/40/50s）
  - 4 个实例全部更新

- **QQ 桥接部署**：Docker 容器已更新，NapCat WebSocket 连接正常

- **涉及文件**：src/zsxq_monitor.py、qq-feishu-bridge/qq_feishu_bridge.py

### 2026-06-04 12:50 · ZSXQ 编辑补图检测 + 服务器部署 + 旧帖误发撤回

- **用户反馈**：击球区小能手的星球最新帖 `82255282581518142`（2026-06-04 11:41）原帖有 1 张图片，但飞书便签图只有文字。
- **根因**：
  - 旧逻辑中帖子一旦标记为 `sent`，后续 API 再返回该帖时不会更新 `topic_json`。
  - 如果作者先发文字、后编辑补图，监控不会重新渲染便签图。
- **手动补发**：
  - 本地只拉最新 1 条，确认 `image_count=1`。
  - 重新生成便签图：`C:\Users\1\.hermes\temp\zsxq_manual_notes\20260604\202606041141.png`。
  - 仅发送到击球区测试飞书群 `oc_7759...`，飞书返回 `message_id=om_x100b6d3bbf3724b0b3c36444e37b960`。
  - 未向正式飞书群补发该测试图。
- **代码修复**：
  - 新增 `content_hash`：按帖子文字、图片 ID、文件 ID、文件名计算内容指纹。
  - `init_db` 自动迁移旧库：新增 `content_hash`、`feishu_message_ids` 字段，并为旧记录回填指纹。
  - 已发送帖子内容变化时，近 24 小时内标记为 `updated`，进入待处理队列并强制重渲染。
  - 历史旧帖内容差异只更新基准指纹，不进入待发送队列，避免正式群收到旧帖补发。
  - `updated` 发送成功后日志为 `TOPIC_RESENT`，并保存多群 `message_id`，便于后续撤回。
- **服务器部署**：
  - 4 个实例均更新：`/opt/zsxq-monitor`、`-ggggg`、`-honghao`、`-kkkkk`。
  - 每个实例旧脚本已备份为 `zsxq_monitor.py.bak-20260604124604`。
  - 4 个实例 `--check` 均为 `ok=10, fail=0`。
  - 4 个实例数据库均已存在 `content_hash=True, feishu_message_ids=True`。
- **误发旧帖处理**：
  - 第一版上线后，部分历史帖子被误判为 `updated` 并触发补发。
  - 已立即停止 timer，补上 24 小时保护规则。
  - 已通过飞书 OpenAPI 撤回误发的历史补发消息：`recall_ok=22, recall_fail=0`。
  - 恢复前确认 4 个实例 `topics_pending=[]`、`files_pending=[]`。
- **恢复与验证**：
  - 4 个 systemd timer 已恢复。
  - 恢复后一轮服务状态：4 个 service 均 `Result=success, ExecMainStatus=0`。
  - 日志确认恢复后只有 `RUN_DONE`/`FETCH_ERR`，没有新的旧帖 `TOPIC_RESENT`。
  - 本地测试：`python -m unittest discover -s tests -p "test_*.py"`，7 个测试通过。
  - 本地语法检查：用 Python `compile()` 直接检查 `src\zsxq_monitor.py`、`tests\test_zsxq_monitor.py`，通过；`py_compile` 在本机写 `__pycache__` 时遇到 Windows 权限问题。
- **Review 查 Bug**：
  - 单纯做内容指纹会把历史库中的旧结构差异误判为编辑，必须加时间窗口。
  - 正式群不能作为测试验证目标；测试补发只允许发测试群。
  - 如果以后需要主动回补正式群，必须由用户明确指定帖子和目标群。
- **第一性原理分析**：
  - 原始需求是“最新帖图片不能漏进便签图”，不是“重发所有变化过的旧帖”。
  - 最小稳定方案是：近期编辑补发、历史内容只校准指纹、所有测试只进测试群。
|- **涉及文件**：src/zsxq_monitor.py、tests/test_zsxq_monitor.py、docs/开发日志.md|

## 2026-06-08

### 09:00 修复 fetch_topics 分页遗漏 Bug + 补发「玻璃基板交流会」

- **问题发现**：2026-06-08 07:43:05 发布的「玻璃基板交流会」（含附件 玻璃基板.docx）未转发到飞书群。
- **根因分析**：
  - ZSXQ API 按 `create_time` 降序返回，`fetch_topics` 用 `batch[-1].create_time > last_seen` 判断是否翻页。
  - API 频繁返回 code 1059 阻断 + 并发定时器冲突，导致 07:43 帖子虽在 API 首页返回，但未被写入 DB。
  - 后续轮询时 `last_seen_time` 已被更新到 07:49:01，07:43 帖子 `create_time < last_seen`，永远无法再被发现。
- **代码修复**：`fetch_topics()` 第 2006-2013 行，将分页终止条件从 `oldest.get("create_time", "") > last_seen`（继续翻页）改为 `oldest_ct <= last_seen`（停止翻页）。原始逻辑是当旧帖进入 batch[-1] 时立即停止分页，但中间的新帖可能因并发竞争未被写入 DB，导致永久遗漏。
- **补发操作**：
  - 将 topic `55522514525281524` 的完整 JSON 写入数据库，status=`discovered`。
  - 运行监控脚本，成功补发便签图 + 附件到正式群和测试群。
- **验证结果**：
  - `TOPIC_SENT: 55522514525281524 /data/zsxq-archive/击球区小能手/20260608/202606080743.png` ✅
  - `FILE_SENT: 55522514525281524:181245512812822 /data/zsxq-archive/击球区小能手/20260608/55522514525281524_玻璃基板.docx` ✅
  - 飞书正式群 message_id: `om_x100b6d55361bc8bcc4f03695a6ce92c` ✅
  - 飞书测试群 message_id: `om_x100b6d553638e4a0c1c160ef369d77e` ✅
- **涉及文件**：`/opt/zsxq-monitor/app/zsxq_monitor.py`（服务器）、`docs/开发日志.md`

### 2026-06-10 13:15 · QQ Bridge 音频转发失败修复（v3.2.1）

详见 `qq-feishu-bridge/docs/开发日志.md` 完整记录。

- **现象**：QQ群 1050711464（赵袭）最后一条音频 `6.10午.mp3` 未转发，DNS 瞬时故障 + 上传无重试。
- **修复**：`fs_upload_media`/`download_qq_url` 加入 3 次瞬时错误重试 + 后台 retry_failed_messages 任务 + raw_json 事件保存。
- **部署**：`docker compose up -d --build bridge`，容器已重建运行。
- **待验证**：需用户在 QQ 群发送新音频测试。

### 2026-06-11 09:00 · ZSXQ Notes 服务宕机 + 本地 Pillow 降级渲染

- **故障现象**：
  - 用户报告击球区小能手星球今天早上有一条帖子更新，飞书群没收到。
  - 实际上帖子是"小球"发布的 talk（topic_id=82255281415455852），因便签渲染失败卡在 `failed` 状态（retry_count=23）。
  - 日志显示 `ConnectionRefusedError: [Errno 111] Connection refused` → `notes.fangyuanxiaozhan.com:443`。

- **根因分析**：
  - 锤子便签渲染服务 `notes.fangyuanxiaozhan.com`（IP: 182.43.25.106）全球不可达，端口 443 拒绝连接。
  - 该服务是 `zhaoolee/fangyuanxiaozhan.com` 的后端 API（`/api/export` 渲染 markdown→PNG，`/api/images/import` 上传图片 CDN）。
  - **所有 4 个 ZSXQ 实例** 完全依赖该服务渲染便签图，服务宕机 → 所有新帖/编辑帖都无法转发。
  - ZSXQ API 从昨晚 23:20 也开始间歇性 1059 限流（与 notes 服务宕机无关，但增加了问题排查难度）。

- **修复内容**：

  | 改进 | 说明 |
  |------|------|
  | `notes_export` 降级 | 优先尝试远程 API（超时从 60s→15s），失败自动切换到本地 Pillow 渲染 |
  | 新增 `local_notes_fallback.py` | Pillow 本地渲染器：自动换行、标题/正文/分隔线/页脚样式、中文字体加载 |
  | `notes_import_image` 容错 | 添加 try/except，远程 CDN 不可用时返回空（不阻断渲染） |
  | `render_topic_note` 图片处理 | `notes_import_image` 返回空时跳过图片（显示 `[图片]` 占位），不再抛异常 |

- **补发操作**：
  - 将 `topic_id=82255281415455852` 状态从 `failed` 重置为 `discovered`。
  - 暂停 `zsxq-poll.timer`（防止并发），手动运行监控脚本。
  - 成功渲染并发送到正式群 + 测试群：
    - 正式群 `oc_ad25b` → `om_x100b6d9431f5f0b4c23b0d640201b1f` ✅
    - 测试群 `oc_7759` → `om_x100b6d9431972d08c1e43d6d60b8b76` ✅
  - 恢复 timer。

- **部署范围**：
  - 修改文件：`zsxq_monitor.py`（notes_export 降级 + notes_import_image 容错 + render_topic_note 图片处理）
  - 新增文件：`local_notes_fallback.py`（Pillow 本地渲染器）
  - 同步到所有 4 个实例：zsxq-monitor / ggggg / kkkkk / honghao
  - 所有 timer 确认 active。

- **已知限制**：
  - 本地降级渲染不支持内嵌图片（图片显示 `[图片]` 占位符），因为图片需要 CDN 公网 URL。
  - 水印功能正常（复用原有 `add_watermark`）。
  - notes 服务恢复后会自动切回远程渲染（无需人工干预）。
  - ZSXQ 1059 限流与此次问题无关，属于间歇性平台限制。

- **避坑规则**：
  - **任何外部 API 依赖必须有降级方案**。notes.fangyuanxiaozhan.com 是个人服务器，随时可能宕机。
  - 降级时要缩短超时（60s→15s），避免每次运行都卡几十秒。
  - 多个外部依赖同时出问题（notes 宕机 + ZSXQ 1059）会导致排查困难，日志要足够清晰。
  - 手动补发时必须暂停 timer，防止并发写 DB 导致数据不一致。
  - 部署到多实例时确保所有实例同步更新（copy 脚本 + fallback 文件）。
  - 降级渲染图片质量不如远程服务，但作为应急方案足够（文字清晰、结构完整）。
|